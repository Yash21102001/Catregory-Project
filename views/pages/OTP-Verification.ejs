<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="apple-touch-icon" sizes="76x76" href="/img/apple-icon.png">
    <link rel="icon" type="image/png" href="/img/favicon.png">
    <title>Enter OTP</title>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11.14.5/dist/sweetalert2.all.min.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/sweetalert2@11.14.5/dist/sweetalert2.min.css" rel="stylesheet">
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background: url('https://images.unsplash.com/photo-1497294815431-9365093b7331?ixlib=rb-1.2.1&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=1950&q=80') no-repeat center center fixed;
            background-size: cover;
            color: #f0f0f0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .container {
            background: rgba(0, 0, 0, 0.7);
            backdrop-filter: blur(10px);
            border-radius: 10px;
            padding: 2rem;
            width: 90%;
            max-width: 400px;
            box-shadow: 0 8px 15px rgba(0, 0, 0, 0.37);
        }

        h1 {
            text-align: center;
            margin-bottom: 1rem;
            font-size: 1.8rem;
            color: #ffffff;
        }

        p {
            text-align: center;
            margin-bottom: 2rem;
            font-size: 1rem;
            color: #dcdcdc;
        }

        form {
            display: flex;
            flex-direction: column;
        }

        .otp-input {
            display: flex;
            justify-content: space-between;
            margin-bottom: 1rem;
        }

        .otp-input input {
            width: 3rem;
            height: 3rem;
            font-size: 1.5rem;
            text-align: center;
            border: none;
            border-radius: 5px;
            background: #333;
            color: #fff;
        }

        button {
            background: #007bff;
            border: none;
            color: white;
            padding: 0.8rem;
            border-radius: 5px;
            font-size: 1rem;
            cursor: pointer;
            transition: background 0.3s;
        }

        button:hover {
            background: #568ce9;
        }

        .back-to-signin {
            text-align: center;
            margin-top: 1rem;
        }

        .back-to-signin a {
            color: #007bff;
            text-decoration: none;
            font-weight: bold;
        }

        .back-to-signin a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Enter OTP</h1>
        <p>We’ve sent a One-Time Password (OTP) to your email. Enter it below to proceed.</p>
        <form action="" method="post">
            <div class="otp-input">
                <input type="text" maxlength="1" required name="otpCode1">
                <input type="text" maxlength="1" required name="otpCode2">
                <input type="text" maxlength="1" required name="otpCode3">
                <input type="text" maxlength="1" required name="otpCode4">
                <input type="text" maxlength="1" required name="otpCode5">
                <input type="text" maxlength="1" required name="otpCode6">
            </div>
            <button type="submit">Verify OTP</button>
        </form>
        <div class="back-to-signin">
            <p><a href="/user/signin">Back To SignIn</a></p>
        </div>
    </div>

    
</body>
</html>
